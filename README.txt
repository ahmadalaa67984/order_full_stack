hello :)  !

(note the application is hosted on server and that test link :https://velents.netlify.app/dashboard)
============================================================================================================
-if you want to run aplication on loaclhost follow that steps:
  npm install 
  npm run dev

- if you want to use localhost for backend to call API's:
  change baseUrl link in src/service/api.js to http://localhost:8800/
  don't forget to run local host for backend 
============================================================================================================
-if you want to run backend on loaclhost follow that steps:
  -download server branch 
  -go to the file directory 
    npm install
    npm run devStart

============================================================================================================
